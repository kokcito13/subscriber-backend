# Production-specific monolog overrides are defined in config/packages/monolog.yaml under the when@prod section.
# This file was created earlier but the project uses a single monolog.yaml with environment-specific sections.
# Keeping this file as a placeholder to avoid accidental duplicate handler definitions.
